package com.example.backend.repository;

import com.example.backend.model.PPS;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PPSRepository extends JpaRepository<PPS, Long> {
}
